
// rounded corners
window-rule {
    geometry-corner-radius 0
    clip-to-geometry true
    draw-border-with-background false
}

window-rule {
    match app-id="brave"
    open-maximized true
}

window-rule {
    match app-id="zen"
    open-maximized true
}

window-rule {
    match app-id="gimp"
    open-maximized true
}

window-rule {
    match app-id="zed"
    open-maximized true
}

window-rule {
    match app-id="brave-chatgpt.com__-Default"
    open-maximized false
}

window-rule {
    match app-id="brave-perplexity.ai__-Default"
    open-maximized false
}

window-rule {
    match app-id="brave-gemini.google.com__-Default"
    open-maximized false
}

window-rule {
    match app-id="brave-aistudio.google.com__-Default"
    open-maximized false
}

window-rule {
    match app-id="brave-web.whatsapp.com__-Default"
    open-maximized false
}

window-rule {
    match app-id="brave-music.youtube.com__-Default"
    open-maximized false
}

window-rule {
    // This regular expression is intentionally made as specific as possible,
    // since this is the default config, and we want no false positives.
    // You can get away with just app-id="wezterm" if you want.
    match app-id=r#"^org\.wezfurlong\.wezterm$"#
    default-column-width {}
}

// Open the Firefox picture-in-picture player as floating by default.
window-rule {
    // This app-id regular expression will work for both:
    // - host Firefox (app-id is "firefox")
    // - Flatpak Firefox (app-id is "org.mozilla.firefox")
    match app-id=r#"firefox$"# title="^Picture-in-Picture$"
    open-floating true
}

// Example: block out two password managers from screen capture.
// (This example rule is commented out with a "/-" in front.)
/-window-rule {
    match app-id=r#"^org\.keepassxc\.KeePassXC$"#
    match app-id=r#"^org\.gnome\.World\.Secrets$"#

    block-out-from "screen-capture"

    // Use this instead if you want them visible on third-party screenshot tools.
    // block-out-from "screencast"
}

// Pacman Install
window-rule {
    match app-id=r#"Alacritty"# title="^pkg-pacman-install$"
    open-floating true
    default-column-width { proportion 0.5; }
    default-window-height { proportion 0.5; }
    default-floating-position x=480 y=270 relative-to="top-left"
    opacity 1.0
}

// AUR Install
window-rule {
    match app-id=r#"Alacritty"# title="^pkg-aur-install$"
    open-floating true
    default-column-width { proportion 0.5; }
    default-window-height { proportion 0.5; }
    default-floating-position x=480 y=270 relative-to="top-left"
    opacity 1.0
}

// Remove Package
window-rule {
    match app-id=r#"Alacritty"# title="^pkg-remove$"
    open-floating true
    default-column-width { proportion 0.5; }
    default-window-height { proportion 0.5; }
    default-floating-position x=480 y=270 relative-to="top-left"
    opacity 1.0
}

// Webapp Install
window-rule {
    match app-id=r#"Alacritty"# title="^webapp-install$"
    open-floating true
    default-column-width { proportion 0.5; }
    default-window-height { proportion 0.5; }
    default-floating-position x=480 y=270 relative-to="top-left"
    opacity 1.0
}

// Webapp Remove
window-rule {
    match app-id=r#"Alacritty"# title="^webapp-remove$"
    open-floating true
    default-column-width { proportion 0.5; }
    default-window-height { proportion 0.5; }
    default-floating-position x=480 y=270 relative-to="top-left"
    opacity 1.0
}

// Update System
window-rule {
    match app-id=r#"Alacritty"# title="^update-system"
    open-floating true
    default-column-width { proportion 0.5; }
    default-window-height { proportion 0.5; }
    default-floating-position x=480 y=270 relative-to="top-left"
    opacity 1.0
}


